<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
   <%= link_to "Navbar", root_path, class: "navbar-brand"  %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
       <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
       <ul class="navbar-nav">
         <li class="nav-item">
           <%= link_to "Tweets", tweets_path, class: "nav-link"  %>
         </li>
              <li class="nav-item">
           <%= link_to "Twitter Accounts", twitter_accounts_path, class: "nav-link"  %>
         </li>
         <li class="nav-item">
           <%= link_to "About", about_path, class: "nav-link"  %>
         </li>
      </ul>


      <ul class="d-flex align-items-center navbar-nav ms-auto mb-2 mb-lg-0 gap-3">
      <% if Current.user %>
     <div class="dropdown">
        <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
          Profile
      </button>
     <ul class="dropdown-menu">
           <%= link_to "Update Password", edit_password_path, class: "dropdown-item" %> 
       <li>
          <hr class="dropdown-divider">
       </li>
       <li class="dropdown-item disabled"><%= Current.user.email %></li>
     </ul>
    </div>
         <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-outline-secondary"  %>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", sign_in_path, class: "nav-link"  %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign Up", sign_up_path, class: "nav-link"  %>
        </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>