<div class="mb-3 card card-body">
   <%= tweet.body %>
   <div class="me-4">
      <%= image_tag tweet.twitter_account.image, class:"rounded-circle " %>
      <%= link_to "#{tweet.twitter_account.name}" , "https://tweet.com/#{tweet.twitter_account.username}", target: :_blank %>

      <% if tweet.published? %>
         <%= link_to "View Tweet","https://twitter.com/#{tweet.twitter_account.username}/status/#{tweet.tweet_id}", terget: :_blank %>
      <% else %>
         Scheduled for <%= l tweet.publish_at, format: :long %>
         <%= link_to "Edit Tweet", edit_tweet_path(tweet)%>
          <%
=begin%>
 <%= link_to "Unschedule Tweet", tweet, method: :delete, form: {data: {turbo_confirm: "Are you sure?"}} %> 
<%
=end%>
      <% end %>
   </div>
</div>